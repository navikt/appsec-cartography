name: Deploy cartography (K8s)
on:
  workflow_dispatch:
   inputs:
     image:
       type: string
       required: true
       description: 'Image to deploy'

jobs:
  deploy:
    permissions:
      contents: read
      id-token: write
      actions: read
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - cluster: dev-fss
            schedule: "0 1 * * *"
            neo4jApp: bolt-proxy-outbound
            hostname: "svc.nais.local"
          - cluster: dev-gcp
            schedule: "0 2 * * *"
            neo4jApp: bolt-proxy-outbound
            hostname: "svc.cluster.local"
          - cluster: prod-fss
            schedule: "0 3 * * *"
            neo4jApp: bolt-proxy-outbound
            hostname: "svc.nais.local"
          - cluster: prod-gcp
            schedule: "0 4 * * *"
            neo4jApp: neo4j
            hostname: "svc.cluster.local"
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
        with:
          persist-credentials: false
      - uses: nais/deploy/actions/deploy@5fdd91aaf69bb0d6dd5e5118cdacb639f487b93b # ratchet:nais/deploy/actions/deploy@v2
        env:
          CLUSTER: ${{ matrix.cluster }}
          RESOURCE: .nais/cartography-k8s.yaml
          IMAGE: ${{ inputs.image }}
          VAR: cluster=${{ matrix.cluster }},schedule=${{ matrix.schedule }},neo4jApp=${{ matrix.neo4jApp }},hostname=${{ matrix.hostname }}
