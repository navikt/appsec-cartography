apiVersion: v1
kind: Service
metadata:
  name: neo4j-bolt
  namespace: appsec
  labels:
    app: neo4j
    team: appsec
spec:
  selector:
    app: neo4j
  ports:
    - name: bolt
      port: 7687
      targetPort: 7687
      protocol: TCP
  type: ClusterIP
